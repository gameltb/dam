/**
 * Shared logic for context menus.
 * Styles are now handled by CSS classes in components.css (.fc-menu-*)
 */

export const handleMouseEnter = () => {
  // Logic handled by CSS classes
};

export const handleMouseLeave = () => {
  // Logic handled by CSS classes
};
